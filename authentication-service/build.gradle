plugins {
    id 'com.palantir.docker' version '0.36.0'
}

docker {
    name "booking-system/authentication-service:${version}"
    files tasks.bootJar.outputs.files
    buildArgs(['JAR_FILE': tasks.bootJar.archiveFileName.get()])
}